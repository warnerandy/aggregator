@require colors;

section.tabs {
	ul {
		list-style: none;
		padding:0;
		margin:0;
		display: flex;
		// justify-content space-around;
		li {
			a {
				padding:8px 16px;
				margin: 0 12px;
				cursor: pointer;
				width:150px;
				text-align center;
				display inline-block;
			}
			&.active {
				padding:8px 16px;
				margin: 0 12px;
				cursor: pointer;
				width:150px;
				text-align: center;
				background-color: blizzBtnBlue;
				border-bottom:2px solid blizzBlueBorder;
				a {
				}
			}
			&:hover {
				a {
					background-color blizzBlueBorder;
				}

			}
		}
	}
	padding: 24px 0 0;
	// border-bottom: 2px solid blizzBlueBorder;
}